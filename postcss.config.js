// Step 1: Install the necessary packages if not already installed
// npm install -D tailwindcss postcss autoprefixer

// Step 2: Initialize Tailwind CSS (if not already done)
// npx tailwindcss init -p

// Step 3: Make sure your postcss.config.js has the right plugins
// postcss.config.js
export default {
    plugins: {
      tailwindcss: {},
      autoprefixer: {},
    },
  }